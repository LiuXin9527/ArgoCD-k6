apiVersion: v1
kind: Pod
metadata:
  name: vol1
spec:
  containers:
    - image: nginx
      name: vm1
      volumeMounts:
        - mountPath: /cache
          name: cache-volume
    - name: vm2
      image: nginx
      volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: cache-volume
  volumes:
    - name: cache-volume
      emptyDir:
        medium: Memory
        sizeLimit: 100Mi